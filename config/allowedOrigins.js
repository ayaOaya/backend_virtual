const allowedOrigins = [
    'http://localhost:3000',
    'https://www.virtualmachine.com',
    'https://virtualmachine.com'
] // here we are determining what hosts have acces to our api

module.exports = allowedOrigins